<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cv" >
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CV" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Estudios"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Certificados"
            android:parentActivityName=".Estudios" />
        <activity
            android:name=".Presentacion"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".DatosPersonales" />
        <activity
            android:name=".DatosPersonales"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>